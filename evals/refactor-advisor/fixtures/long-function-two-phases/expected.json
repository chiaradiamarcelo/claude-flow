{
  "fixture": "long-function-two-phases",
  "description": "A single method well over ~15 lines with two distinct phases — totaling the transactions, then rendering the statement text. This is the 'Compose method' opportunity: extract each phase into a named helper so the top-level method reads as a table of contents. The advisor must flag the long, multi-phase function.",
  "applicableAgents": ["refactor-advisor"],
  "agents": {
    "refactor-advisor": {
      "expectedStatus": "FAIL",
      "mustMention": ["compose"]
    }
  }
}
