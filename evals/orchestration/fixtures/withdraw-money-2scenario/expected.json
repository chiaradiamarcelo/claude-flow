{
  "fixture": "withdraw-money-2scenario",
  "description": "Multi-scenario choreography. The pre-seeded spec has TWO unchecked scenarios; the real `/run-pipeline withdraw-money` must implement them ONE AT A TIME, top-to-bottom (architect→developer per scenario), THEN run reviewers ONCE over the combined result, then the fix-loop. Fake workers self-log; the count-aware fake architect writes SCENARIO-0N per call; the fake test-reviewer forces a FAIL on the first review. We assert two architect→developer cycles before any review (no batching, no reviewing mid-feature), the forced fix pass, and that it ends after a passing review. maxArchitects=2 catches a re-planning loop. This is the N>1 case every other fixture omits.",
  "applicableAgents": ["orchestration"],
  "orchestratorPrompt": "/run-pipeline withdraw-money\n\nUse the project's architect, developer, and reviewer sub-agents — do not do their work yourself. Work directly in this directory; skip any worktree step. Run non-interactively: do not ask questions and auto-continue.",
  "orchestration": {
    "logFile": "pipeline-calls.log",
    "expectedSubsequence": ["architect", "developer:impl", "architect", "developer:impl", "test-reviewer", "developer:fix", "test-reviewer"],
    "endsAfterReview": true,
    "maxArchitects": 2
  }
}
