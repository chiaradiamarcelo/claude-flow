{
  "description": "The fake is reused across tests and cleaned via a `reset()` call in setup. The skill requires a fresh fake instance created in the before-each hook \u2014 no `clear()`/`reset()` methods on fakes. The reviewer must flag the reset()-based clean-slate.",
  "given": {
    "files": "input/"
  },
  "when": {
    "do": "agent",
    "agent": "test-reviewer"
  },
  "then": {
    "grader": "verdict",
    "expectedStatus": "FAIL",
    "mustMention": [
      "reset"
    ]
  }
}
