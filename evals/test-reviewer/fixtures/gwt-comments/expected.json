{
  "fixture": "gwt-comments",
  "description": "A structurally fine test, except the Given-When-Then sections are labelled with `// Given` / `// When` / `// Then` comments. The skill mandates GWT separated by blank lines WITHOUT comments. The reviewer must flag the comments.",
  "applicableAgents": ["test-reviewer"],
  "agents": {
    "test-reviewer": {
      "expectedStatus": "FAIL",
      "mustMention": ["comment"]
    }
  }
}
