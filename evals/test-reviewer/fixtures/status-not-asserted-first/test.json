{
  "description": "A controller slice test (`...IT`) asserts the response body before the HTTP status. The skill's controller baseline requires asserting HTTP status first, then payload/headers. The reviewer must flag the assertion ordering.",
  "given": {
    "files": "input/"
  },
  "when": {
    "do": "agent",
    "agent": "test-reviewer"
  },
  "then": {
    "grader": "verdict",
    "expectedStatus": "FAIL",
    "mustMention": [
      "status"
    ],
    "mustInvokeSkills": [
      "testing"
    ]
  }
}
