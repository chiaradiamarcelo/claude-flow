{
  "fixture": "trivial-getter-test",
  "description": "The test exercises a trivial getter with no logic (`account.id` returns the constructor argument). The skill's 'What NOT to test' lists trivial getters/setters with no logic. The reviewer must flag this as something not worth testing.",
  "applicableAgents": ["test-reviewer"],
  "agents": {
    "test-reviewer": {
      "expectedStatus": "FAIL",
      "mustMention": ["getter"]
    }
  }
}
