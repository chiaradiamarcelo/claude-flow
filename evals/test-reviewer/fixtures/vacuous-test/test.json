{
  "description": "A vacuous test: it only asserts the returned account `isNotNull()`, which passes regardless of whether the account was created correctly. The skill says to delete vacuous tests (a test that passes regardless of correctness gives false confidence). The reviewer must flag it.",
  "given": {
    "files": "input/"
  },
  "when": {
    "do": "agent",
    "agent": "test-reviewer"
  },
  "then": {
    "grader": "verdict",
    "expectedStatus": "FAIL",
    "mustMention": [
      "vacuous"
    ]
  }
}
