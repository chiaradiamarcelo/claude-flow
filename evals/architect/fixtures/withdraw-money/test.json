{
  "description": "A write-side command scenario (withdraw from an account aggregate). The architect must produce a SCENARIO-01 plan that: writes only the plan .md (no code); is an ordered checklist driven inside-out (the use-case test precedes the controller step); plans a contract test for the Repository port; and plans an equality test for the identity-bearing Account entity. We assert only those coarse, stable facts \u2014 not the exact wording.",
  "given": {
    "files": "input/"
  },
  "when": {
    "do": "agent",
    "agent": "architect",
    "prompt": "Plan SCENARIO-01 for the feature withdraw-money. The specification is at docs/specifications/withdraw-money/specification.md. Write the scenario plan file and nothing else."
  },
  "then": {
    "grader": "plan",
    "planMustExist": true,
    "writesNoCode": true,
    "minSteps": 5,
    "mustMention": [
      "contract",
      "equality"
    ],
    "orderedBefore": [
      [
        "Test",
        "Controller"
      ]
    ]
  }
}
