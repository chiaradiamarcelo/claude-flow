{
  "description": "A write-side command scenario (withdraw from an account aggregate). The architect now writes a declarative `## Structure & Contracts` skeleton (NOT a checklist, and NOT the test list). We assert coarse, stable facts: it writes only the plan .md (no code); declares the Repository port's contract-test obligation; flags the identity-bearing aggregate's equality requirement; and does NOT enumerate the ordered test list (that is the test-designer's job, a later step). Wording is not asserted.",
  "given": { "files": "input/" },
  "when": {
    "do": "agent",
    "agent": "architect",
    "prompt": "Plan SCENARIO-01 for the feature withdraw-money. The specification is at docs/specifications/withdraw-money/specification.md. Write the scenario plan file and nothing else."
  },
  "then": {
    "grader": "plan",
    "planMustExist": true,
    "writesNoCode": true,
    "minSteps": 4,
    "mustMention": ["Structure & Contracts", "contract", "equality"],
    "mustNotMention": ["Ordered Test List", "| TPP |"]
  }
}
