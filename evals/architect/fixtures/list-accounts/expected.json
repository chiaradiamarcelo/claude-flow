{
  "fixture": "list-accounts",
  "description": "A read-side query scenario (list account summaries — a pure read, no aggregate, no state change). The architect must plan it on the read side: a Query-named port (the clean-architecture skill reserves 'Query' for read-side ports), a contract test for that port's fake, and only the plan .md written. This pins that the architect applies the CQRS read-side path rather than inventing a write-side Repository + use case.",
  "applicableAgents": ["architect"],
  "feature": "list-accounts",
  "scenario": "SCENARIO-01",
  "prompt": "Plan SCENARIO-01 for the feature list-accounts. The specification is at docs/specifications/list-accounts/specification.md. Write the scenario plan file and nothing else.",
  "agents": {
    "architect": {
      "planMustExist": true,
      "writesNoCode": true,
      "minSteps": 4,
      "mustMention": ["Query", "contract"]
    }
  }
}
