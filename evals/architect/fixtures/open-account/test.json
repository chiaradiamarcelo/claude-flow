{
  "description": "A write-side create scenario that introduces a new HTTP endpoint. Beyond the write-side basics (Repository contract-test obligation, identity equality flagged, no code written, declarative structure not a checklist), the architect must reflect the api-conventions skill in the API-surface bullet: a create returns 201 with a Location header. It must NOT emit the ordered test list. We assert those facts coarsely. (If 'Location' is absent, that is a genuine finding about the architect not applying the 201+Location rule.)",
  "given": { "files": "input/" },
  "when": {
    "do": "agent",
    "agent": "architect",
    "prompt": "Plan SCENARIO-01 for the feature open-account. The specification is at docs/specifications/open-account/specification.md. Write the scenario plan file and nothing else."
  },
  "then": {
    "grader": "plan",
    "planMustExist": true,
    "writesNoCode": true,
    "minSteps": 4,
    "mustMention": ["Structure & Contracts", "contract", "equality", "201", "Location"],
    "mustNotMention": ["Ordered Test List", "| TPP |"]
  }
}
