{
  "description": "A thin, RESTfully-routed create endpoint that returns a response DTO but defaults to 200 OK on resource creation \u2014 it should respond 201 Created. The reviewer must fail it and flag the semantically wrong status code (should be 201).",
  "given": {
    "files": "input/"
  },
  "when": {
    "do": "agent",
    "agent": "api-reviewer"
  },
  "then": {
    "grader": "verdict",
    "expectedStatus": "FAIL",
    "mustMention": [
      "201"
    ]
  }
}
