{
  "description": "The Account aggregate package imports from a sibling aggregate package (domain/models/loan/Loan) and reaches into it directly. One aggregate package must never import from another \u2014 cross-aggregate orchestration belongs in a use case that holds both Repositories. The reviewer must fail it and flag the cross-aggregate import.",
  "given": {
    "files": "input/"
  },
  "when": {
    "do": "agent",
    "agent": "arch-reviewer"
  },
  "then": {
    "grader": "verdict",
    "expectedStatus": "FAIL",
    "severities": {
      "VIOLATION": {
        "min": 1
      }
    },
    "mustMention": [
      "aggregate"
    ],
    "mustInvokeSkills": [
      "clean-architecture"
    ]
  }
}
