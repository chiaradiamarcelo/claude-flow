{
  "description": "A domain aggregate that imports an API-layer request DTO (banking.api.dto.OpenAccountRequest) and uses it in a factory. The domain must not depend on the delivery layer's DTOs \u2014 dependencies point inward. The reviewer must fail it and flag the DTO leaking into the domain.",
  "given": {
    "files": "input/"
  },
  "when": {
    "do": "agent",
    "agent": "arch-reviewer"
  },
  "then": {
    "grader": "verdict",
    "expectedStatus": "FAIL",
    "severities": {
      "VIOLATION": {
        "min": 1
      }
    },
    "mustMention": [
      "DTO"
    ],
    "mustInvokeSkills": [
      "clean-architecture"
    ]
  }
}
