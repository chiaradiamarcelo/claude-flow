{
  "description": "A use case class (TransferMoneyUseCase, orchestrating across aggregates) declared in the domain package (banking.domain.models.account) instead of the application layer. Use cases live in application/, not in domain. The reviewer must fail it and flag the misplaced file \u2014 it belongs in the application layer.",
  "given": {
    "files": "input/"
  },
  "when": {
    "do": "agent",
    "agent": "arch-reviewer"
  },
  "then": {
    "grader": "verdict",
    "expectedStatus": "FAIL",
    "severities": {
      "VIOLATION": {
        "min": 1
      }
    },
    "mustMention": [
      "application"
    ]
  }
}
