{
  "description": "An API controller that injects a concrete infrastructure adapter (AccountPostgresAdapter) directly instead of an application use case or a domain read-side port. The API layer must not reach into infrastructure internals. The reviewer must fail it and flag the controller depending on the adapter.",
  "given": {
    "files": "input/"
  },
  "when": {
    "do": "agent",
    "agent": "arch-reviewer"
  },
  "then": {
    "grader": "verdict",
    "expectedStatus": "FAIL",
    "mustMention": [
      "adapter"
    ],
    "mustInvokeSkills": [
      "clean-architecture"
    ]
  }
}
