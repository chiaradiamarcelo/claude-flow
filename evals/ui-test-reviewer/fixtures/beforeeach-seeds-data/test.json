{
  "description": "A component test whose beforeEach does the right thing (clearAllMocks) but also seeds test data (the `items` array). beforeEach should reset stateless dependencies and mocks only \u2014 never seed test data, which belongs visible in each test's Given. The reviewer must fail it and flag the beforeEach seeding test data.",
  "given": {
    "files": "input/"
  },
  "when": {
    "do": "agent",
    "agent": "ui-test-reviewer"
  },
  "then": {
    "grader": "verdict",
    "expectedStatus": "FAIL",
    "mustMention": [
      "beforeEach"
    ],
    "mustInvokeSkills": [
      "ui-testing",
      "testing"
    ]
  }
}
