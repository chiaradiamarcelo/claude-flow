{
  "description": "A clean component test that queries with getByTestId for content that an accessible getByRole('alert') query would reach. getByTestId is the last-resort query; getByRole comes first. The reviewer must fail it and recommend the getByRole query.",
  "given": {
    "files": "input/"
  },
  "when": {
    "do": "agent",
    "agent": "ui-test-reviewer"
  },
  "then": {
    "grader": "verdict",
    "expectedStatus": "FAIL",
    "mustMention": [
      "getByRole"
    ]
  }
}
