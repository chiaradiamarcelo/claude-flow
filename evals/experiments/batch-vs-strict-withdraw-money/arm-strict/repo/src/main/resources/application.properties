# In-memory H2 so the build is self-contained (no external database).
spring.datasource.url=jdbc:h2:mem:bank;DB_CLOSE_DELAY=-1
# Schema is owned by Flyway migrations; Hibernate only validates against them.
spring.jpa.hibernate.ddl-auto=validate
