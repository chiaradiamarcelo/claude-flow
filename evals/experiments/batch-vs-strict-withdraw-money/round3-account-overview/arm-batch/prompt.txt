Implement SCENARIO-01 of the account-overview feature in THIS repository (a Kotlin / Spring Boot 4 project, base package `com.example.bank`). This is a READ-SIDE (CQRS query) slice — a Query + read model, no aggregate/UseCase.

The plan is at `docs/specifications/account-overview/SCENARIO-01.md`. Read it fully first: it contains the scenario, the architect's "Structure & Contracts", and a 7-row "Ordered Test List". The specification is at `docs/specifications/account-overview/specification.md`. Do NOT modify either file.

Execution discipline — BATCH-PER-CLASS TDD. You hold the whole plan in context, so batch at CLASS granularity instead of one test at a time:
- Group the Ordered Test List by the class each row exercises, and take the classes in the plan's order: `AccountTierTest`, `AccountOverviewQuery` contract (+ its fake spec and the JPA adapter spec that run it), `AccountOverviewControllerIT`.
- For EACH class, in order:
  1. Write ALL of that class's tests at once (keeping the plan's row order and justifications).
  2. Run `./gradlew test` ONCE and verify BATCH-RED: every new test must fail for the reason the plan states. This guardrail is NON-NEGOTIABLE — if any new test is GREEN on this first run, it is vacuous; fix it (make it actually exercise the behaviour) BEFORE writing any production code.
  3. Then write the production code for that class to make all its tests pass; run `./gradlew test` ONCE and confirm green.
  4. Refactor if needed while staying green, then move to the next class.

Conventions: follow the project's Clean Architecture, cqrs, testing, and tdd skills (load them). Implement idiomatic Kotlin. Test naming: unit `<ClassName>Test`, controller slice `<ControllerName>IT`. Money is `BigDecimal`.

Definition of done: all 7 rows implemented and `./gradlew test` is fully green. Do not modify the plan or spec files. Do not open PRs or commit.
